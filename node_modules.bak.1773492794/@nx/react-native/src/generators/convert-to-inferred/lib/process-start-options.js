"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.processStartOptions = processStartOptions;
function processStartOptions(_projectName, options, _migrationLogs) {
    const args = [];
    for (const k of Object.keys(options)) {
        if (k === 'resetCache') {
            if (options[k] === true) {
                args.push(`--reset-cache`);
            }
        }
        else if (k === 'interactive') {
            if (options[k] === false) {
                args.push(`--no-interactive`);
            }
        }
        else {
            args.push(`--${k}`, options[k]);
        }
        delete options[k];
    }
    options.args = args;
}
